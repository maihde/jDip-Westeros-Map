Installation
============
Copy westerosVariants.zip into the variants folder of your jDip install.

Tested with jDip 1.6.8

Updating the map
================
Use prov.svg to place unit locations, supply-centers, and text.  You must keep the file in plain .svg format.  You can then run:

    $ prov2map.py prov.svg

To produce jDip province meta-data and text layer.
